// Graph node dimensions and epic-region padding. Tuned values carried over from
// the validated prototype — the epic bounding box and layout maths depend on
// these, so keep them in one place.
export const CARD_W = 200
export const CARD_H = 104
export const ACTOR_W = 132
export const ACTOR_H = 44
export const EPIC_REGION_PAD = 32
export const EPIC_REGION_LABEL_OFFSET = 20
